class Solution {
    public boolean uniformArray(int[] nums1) {
        // since if all are even then copy it same for odd
        //if at leat one is even copy the odd one and -1 from even to make it odd
        return true;
    }
}